feat: Implement Phase 1 & 2 improvements

Security (#2):
- Remove hardcoded credentials from appsettings.json
- Add environment variable support (${DB_CONNECTION_STRING})
- Add appsettings.Development.json for local development
- Add Serilog package references

Logging (#3):
- Add Serilog with Console and File sinks
- Add structured logging to all services and commands
- Add rolling file logs (7-day retention)
- Add error handling with stack traces

Reconnection (#1):
- Add automatic reconnection with exponential backoff
- Add max retry count (5 attempts)
- Add reconnecting status callback
- Add UI indicator for reconnecting state

CI/CD (#4):
- Add GitHub Actions workflow for server (dotnet build)
- Add GitHub Actions workflow for client (MSBuild)

TLS (#5):
- Add TLS configuration template in appsettings.json
- Add TLS_SETUP.md documentation
